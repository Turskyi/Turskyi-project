
import 'package:flutter/cupertino.dart';

class AppDimens {
  /* insets */
  static const EdgeInsets insetsAll4 = EdgeInsets.all(4);
  static const EdgeInsets insetsAll12 = EdgeInsets.all(12);
  static const EdgeInsets insetsAll24 = EdgeInsets.all(24.0);
  static const EdgeInsets insetsAll64 = EdgeInsets.all(64.0);

  /* radii */
  static const radiusBlur = 12.0;
  static BorderRadius radiusButton = BorderRadius.circular(20.0);

  /* widths */
  static const widthGooglePlayButton = 140.0;

  /* paddings */
  static const paddingTopGitHub = 10.0;

  /* heights */
  static const heightImageButton = 40.0;

  /* opacities */
  static const opacityShadow = 0.6;

  /* offsets */
  static Offset offsetShadow = Offset(0, 0);

  /* elevations */
}
