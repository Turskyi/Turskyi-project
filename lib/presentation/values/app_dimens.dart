import 'package:flutter/cupertino.dart';

class AppDimens {
  // insets
  static const EdgeInsets insetsAll4 = const EdgeInsets.all(4);
  static const EdgeInsets insetsAll12 = const EdgeInsets.all(12);
  static const EdgeInsets insetsAll24 = const EdgeInsets.all(24.0);
  static const EdgeInsets insetsAll64 = const EdgeInsets.all(64.0);

  // radii
  static const double radiusBlur = 12.0;

  // border radii
  static final BorderRadius radiusButton = BorderRadius.circular(20.0);

  // widths
  static const double widthGooglePlayButton = 140.0;
  static const double widthFacebook = 68.0;

  // paddings
  static const double paddingTopGitHub = 10.0;

  // heights
  static const double heightImageButton = 40.0;

  // opacities
  static const double opacityShadow = 0.6;
}
