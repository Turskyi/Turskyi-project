name: Build & upload to GitHub pages

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1 # If you're using actions/checkout@v2 you must set persist-credentials to false in most cases for the deployment to work correctly.
        with:
          persist-credentials: false

      - name: Install and Build 🔧 # This example project is built using npm and outputs the result to the 'build' folder. Replace with the commands required to build your project, or remove this step entirely if your site is pre-built.
      - run: flutter config --enable-web
      - run: flutter pub get
      - run: flutter build web

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@3.6.2
        with:
          GITHUB_TOKEN: ${{ secrets.ACCESS_TOKEN }}
          BRANCH: master # The branch the action should deploy to.
          FOLDER: build/web # The folder the action should deploy.
          CLEAN: true # Automatically remove deleted files from the deploy branch

#jobs:
#  build:
#    name: Build Flutter (Web)
#    env:
#      my_secret: ${{secrets.ACCESS_TOKEN}}
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v1
#      - uses: subosito/flutter-action@v1
#        with:
#          channel: 'dev'
#      - run: flutter config --enable-web
#      - run: flutter pub get
#      - run: flutter build web
#      - run: cd build/web
#      - run: git init
#      - run: git config --global user.name  "Dmytro Turskyi"
#      - run: git config --global user.email "dmitriy.turskiy@gmail.com"
#      - run: git status
#      - run: git remote add secure-origin https://${{ secrets.ACCESS_TOKEN }}@github.com/Turskyi/turskyi.github.io.git
#      - run: git checkout -b master
#      - run: git add .
#      - run: git commit -m "Updated personal website"
#      - run: git push --force secure-origin master
