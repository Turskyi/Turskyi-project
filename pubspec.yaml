name: turskyi
description: Personal web page of Dmytro Turskyi.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
homepage: https://turskyi.com

version: 1.0.9

environment:
  sdk: '>=3.9.2 <4.0.0'

dependencies:
  dio: ^5.9.2
  email_validator: ^3.0.0
  envied: ^1.3.4
  envied_generator: ^1.3.4
  flutter:
    sdk: flutter

  # display svg images
  flutter_svg: ^2.2.4

  #  localization
  flutter_translate:
    git:
      url: https://github.com/Javernaver/flutter_translate.git
      ref: master

  intl: ^0.20.2
  mockito: ^5.6.4

  #  Add ChangeNotifierProvider and Consumer for implementing the MVP structural
  #  design pattern.
  provider: ^6.1.5+1

  resend: ^1.0.0
  shared_preferences: ^2.5.5
  url_launcher: ^6.3.2

dev_dependencies:
  build_runner: ^2.13.1
  flutter_test:
    sdk: flutter

  # The set of rules follows the Effective Dart: Style Guide.
  lint: ^2.8.0

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons' font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # assets of the application
  assets:
    #    strings
    - assets/i18n/
    #    picture files
    - assets/images/
    - assets/images/feature_graphics/


